# C0：框架更新检测（🚫 硬阻断，不可跳过）

## 检查逻辑

三层检测：L1 结构签名（H2/H3 标题）→ L2 字段/路径（表格列数、引用路径）→ L3 条目标记（📖 条目）。任一文件有 🔴 → 🚫 硬阻断。

## 执行步骤

1. 扫描 `framework/templates/` 与 `{draft_dir 或 novel/}` 对比
2. 任一模板签名在实例中不匹配 → 🔴 升级为 🚫
3. 输出差异明细 + 提示执行 `migration-keeper` Skill (migrate-project)

## 降级

- 框架版本完全一致 → 静默通过
- 仅 🟡 项 → 输出提醒，不阻断
