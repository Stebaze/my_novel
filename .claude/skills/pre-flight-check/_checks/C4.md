# C4：前一章变更记录完整性

**条件**：N > 1，🚫 **硬阻断**

## 执行

```
effective_prev = C3.6 定位的 target

检查 _changes.md 中 Ch{effective_prev} 记录：
├── 有记录 → ✅ 通过
└── 无记录 → 🚫 阻断：
    "Ch{effective_prev} 已写作但无变更记录——请调 settings-manager (record-settings) 补齐"
```

## 标注

如 `effective_prev < N-1` → 报告中注明 "C4 以 Ch{effective_prev} 为准（Ch{N-1} 未写）"。
