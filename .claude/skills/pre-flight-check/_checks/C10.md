# C10：书级设定派发状态检查

**条件**：scope="writing" 且 outline.md L1-L3 已填实（`workflow_position: outline-tingle-step2-done`），⚠️ **软提醒不阻断**

> outline-tingle 2.8.5 步骤把 `outline.md` L1-L3 已定型字段派发到草稿目录的 `characters/`/`character-arcs.md`/`world/rules.md`/`thread-map.md`，让下游 plan-chapter 阶段 1 设定快照能读到非空骨架。本检查检测派发状态字段 `book_settings_dispatched`——未派发时仅 ⚠️ 提醒，不阻断（`outline.md` 仍是真相源，下游可降级只读 outline）。

## 步骤 1：触发判定

```
读取 {draft_dir}/outline.md（无草稿降级 novel/outline.md）
├── 文件不存在 → 跳过 C10（C3 已报缺失，本检查不重复）
├── frontmatter YAML 解析失败 → ⚠️ 提示"outline.md frontmatter 损坏，C10 跳过"，结束
├── frontmatter `workflow_position` ≠ "outline-tingle-step2-done" → 跳过 C10
│   （大纲尚未形成，派发步骤尚未到达——C9 会先报 🟡，本检查不重复）
└── frontmatter `workflow_position` = "outline-tingle-step2-done" → 进入步骤 2
```

## 步骤 2：派发状态检测

```
读取 frontmatter `book_settings_dispatched` 字段：
├── = true → ✅ 通过（report "书级设定已派发：characters/character-arcs/world/thread-map 含骨架"）
├── = false / 缺失 / 非 true → ⚠️ 软提醒：
    "outline.md L1-L3 已填实但书级设定未派发到设定文件
     （characters/character-arcs/world/thread-map 这些文件可能为空/模板默认），
     下游 plan-chapter 阶段 1 设定快照读这些文件时返回空，仅 outline.md 可读。
     如需补派发：重跑 /outline-tingle continue（断点续传到 2.8.5）或手动按
     outline-tingle SKILL.md §2.8.5 Q3 映射表派发。"
    （不阻断，仅提示）
```

## 步骤 3：修复路径

C10 是 ⚠️ 软提醒，不进修复路径主链。修复入口记录在提醒文本中：

| 路径 | 调用 |
|------|------|
| 断点续传重跑派发 | `/outline-tingle continue`（outline-tingle 检测 `workflow_position: outline-tingle-step2-done` 时提示"大纲已形成"，需用户/本检查显式说明要重跑 2.8.5 派发） |
| 手动派发 | 按 outline-tingle SKILL.md §2.8.5 Q3 映射表把 outline.md L1-L3 字段写入草稿目录对应设定文件段，置 `book_settings_dispatched: true` |

> C10 不阻断，修复路径不进 pre-flight-check 修复主链（≤4 步）；提醒文本自带修复入口指引，作者按需手动触发。

## 降级

- `outline.md` 文件缺失 → 跳过 C10（C3 已报 🚫/⚠️，本检查不重复）
- frontmatter YAML 解析失败 → ⚠️ 提示"outline.md frontmatter 损坏，无法判读 `book_settings_dispatched`，C10 跳过"
- frontmatter 缺 `book_settings_dispatched` 字段（旧 outline） → 视同 `false`，按步骤 2 ⚠️ 软提醒；提示"outline.md frontmatter 缺 `book_settings_dispatched` 字段，建议调 `migration-keeper` (migrate-project) 补齐 v2 模板的第 8 字段"
