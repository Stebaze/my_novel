# C3.4：frontmatter 准确性提示

**条件**：C3.3 通过，**仅 ⚠️**

## 执行

1. 从 frontmatter 提取 heading 列表
2. grep 文件中实际 H2 标题（`## `）
3. frontmatter 中有、文件中无 → ⚠️ "frontmatter 引用了不存在的标题：[heading]"
4. 文件中有、frontmatter 中无 → ⚠️ "文件存在未索引的 H2 标题：[heading]"

## 注意

不影响通过判定——仅作提醒，不阻断。
